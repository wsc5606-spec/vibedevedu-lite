{
  "language": "rust",
  "chapters": [
    {
      "chapterId": 1,
      "title": "1단원: 출력과 주석",
      "content": "# 1단원: 출력과 주석\n\n## Rust 프로그램의 기본 구조\n\nRust 프로그램은 `main` 함수에서 시작됩니다:\n\n```rust\nfn main() {\n    println!(\"Hello, world!\");\n}\n```\n\n- `fn main()`: 프로그램의 진입점 (entry point)\n- 중괄호 `{}` 안에 코드 작성\n- 세미콜론 `;`으로 문장 종료\n\n## 출력하기\n\nRust는 매크로를 사용하여 출력합니다:\n\n```rust\nprintln!(\"텍스트\");      // 줄바꿈 포함\nprint!(\"텍스트\");        // 줄바꿈 없음\n```\n\n**형식화된 출력:**\n\n```rust\nlet name = \"철수\";\nlet age = 25;\nprintln!(\"이름: {}, 나이: {}\", name, age);\nprintln!(\"이름: {name}, 나이: {age}\");  // 이름 지정\n```\n\n## 주석\n\n**한 줄 주석:**\n\n```rust\n// 이것은 주석입니다\n```\n\n**여러 줄 주석:**\n\n```rust\n/*\n  여러 줄에 걸친\n  주석입니다\n*/\n```\n\n**문서 주석:**\n\n```rust\n/// 함수나 모듈을 설명하는 문서 주석\n/// 여러 줄로 작성 가능\n```\n\n## 핵심 요약\n\n- **프로그램 시작**: `fn main() { }` 함수\n- **출력**: `println!(\"텍스트\")` (줄바꿈 O), `print!(\"텍스트\")` (줄바꿈 X)\n- **형식화**: `println!(\"값: {}\", 변수)` 또는 `println!(\"값: {변수}\")`\n- **주석**: `//` (한 줄), `/* */` (여러 줄), `///` (문서)\n- **매크로**: Rust에서 `!`가 붙은 것은 매크로 (예: `println!`)"
    },
    {
      "chapterId": 2,
      "title": "2단원: 변수와 데이터 타입",
      "content": "# 2단원: 변수와 데이터 타입\n\n## 변수 선언\n\nRust의 변수는 기본적으로 **불변(immutable)**입니다:\n\n```rust\nlet x = 5;           // 불변 변수\nlet mut y = 10;      // 가변 변수\ny = 15;              // OK\n// x = 10;           // 에러! x는 불변\n```\n\n**변수 재선언 (Shadowing):**\n\n```rust\nlet x = 5;\nlet x = x + 1;       // 새로운 변수 x (타입 변경 가능)\nlet x = \"문자열\";     // 타입도 변경 가능\n```\n\n## 데이터 타입\n\n**정수형:**\n\n```rust\nlet a: i32 = 100;    // 부호 있는 32비트 정수 (기본)\nlet b: u32 = 200;    // 부호 없는 32비트 정수\nlet c: i64 = 1000;   // 64비트\nlet d: isize = 50;   // 플랫폼 의존적 크기\n```\n\n**실수형:**\n\n```rust\nlet x: f32 = 3.14;   // 32비트 부동소수점\nlet y: f64 = 2.718;  // 64비트 (기본)\n```\n\n**불린형:**\n\n```rust\nlet is_active: bool = true;\nlet is_done = false;\n```\n\n**문자형:**\n\n```rust\nlet c: char = 'A';   // 유니코드 문자 (4바이트)\nlet emoji = '😊';\n```\n\n**문자열:**\n\n```rust\nlet s1: &str = \"문자열 슬라이스\";  // 불변\nlet s2: String = String::from(\"소유 문자열\");  // 가변\n```\n\n**타입 추론:**\n\nRust는 대부분의 경우 타입을 자동으로 추론합니다:\n\n```rust\nlet number = 42;         // i32로 추론\nlet pi = 3.14;           // f64로 추론\nlet text = \"hello\";      // &str로 추론\n```\n\n## 상수\n\n```rust\nconst MAX_POINTS: u32 = 100_000;  // 타입 명시 필수\n```\n\n- 항상 불변\n- 타입을 반드시 명시해야 함\n- 컴파일 타임에 값이 결정되어야 함\n\n## 핵심 요약\n\n- **변수**: `let x = 값;` (불변), `let mut x = 값;` (가변)\n- **불변성**: Rust의 기본 특성, 안전성 보장\n- **Shadowing**: 같은 이름으로 새 변수 선언 가능\n- **정수**: `i32`, `u32`, `i64`, `u64` 등\n- **실수**: `f32`, `f64` (기본)\n- **불린**: `bool` (`true`, `false`)\n- **문자**: `char` (유니코드 4바이트)\n- **문자열**: `&str` (불변), `String` (소유)\n- **상수**: `const 이름: 타입 = 값;`"
    },
    {
      "chapterId": 3,
      "title": "3단원: 연산자와 입력",
      "content": "# 3단원: 연산자와 입력\n\n## 산술 연산자\n\n```rust\nlet sum = 5 + 3;        // 8\nlet diff = 10 - 4;      // 6\nlet product = 3 * 4;    // 12\nlet quotient = 10 / 3;  // 3 (정수 나눗셈)\nlet remainder = 10 % 3; // 1 (나머지)\n```\n\n## 비교 연산자\n\n```rust\nlet a = 5;\nlet b = 10;\n\na == b;  // false (같음)\na != b;  // true (다름)\na < b;   // true (작음)\na > b;   // false (큼)\na <= b;  // true (작거나 같음)\na >= b;  // false (크거나 같음)\n```\n\n## 논리 연산자\n\n```rust\nlet t = true;\nlet f = false;\n\nt && f;  // false (AND)\nt || f;  // true (OR)\n!t;      // false (NOT)\n```\n\n## 비트 연산자\n\n```rust\nlet a = 0b1010;  // 10 (2진수)\nlet b = 0b1100;  // 12\n\na & b;   // 0b1000 (8) - AND\na | b;   // 0b1110 (14) - OR\na ^ b;   // 0b0110 (6) - XOR\n!a;      // 비트 반전\na << 1;  // 0b10100 (20) - 왼쪽 시프트\na >> 1;  // 0b101 (5) - 오른쪽 시프트\n```\n\n## 복합 대입 연산자\n\n```rust\nlet mut x = 5;\nx += 3;   // x = x + 3 → 8\nx -= 2;   // x = x - 2 → 6\nx *= 4;   // x = x * 4 → 24\nx /= 3;   // x = x / 3 → 8\nx %= 5;   // x = x % 5 → 3\n```\n\n## 사용자 입력 받기\n\nRust에서 표준 입력을 받으려면 `std::io` 모듈을 사용합니다:\n\n```rust\nuse std::io;\n\nfn main() {\n    let mut input = String::new();\n    \n    println!(\"입력하세요:\");\n    io::stdin()\n        .read_line(&mut input)\n        .expect(\"입력 실패\");\n    \n    println!(\"입력한 값: {}\", input.trim());\n}\n```\n\n**숫자 입력 받기:**\n\n```rust\nuse std::io;\n\nlet mut input = String::new();\nio::stdin().read_line(&mut input).expect(\"입력 실패\");\n\nlet number: i32 = input.trim().parse().expect(\"숫자가 아님\");\nprintln!(\"입력한 숫자: {}\", number);\n```\n\n## 타입 변환\n\n```rust\nlet a = 10;\nlet b = a as f64;        // 정수를 실수로\n\nlet s = \"42\";\nlet n: i32 = s.parse().unwrap();  // 문자열을 정수로\n```\n\n## 핵심 요약\n\n- **산술**: `+`, `-`, `*`, `/`, `%`\n- **비교**: `==`, `!=`, `<`, `>`, `<=`, `>=`\n- **논리**: `&&` (AND), `||` (OR), `!` (NOT)\n- **비트**: `&`, `|`, `^`, `!`, `<<`, `>>`\n- **복합 대입**: `+=`, `-=`, `*=`, `/=`, `%=`\n- **입력**: `io::stdin().read_line(&mut 변수)`\n- **파싱**: `.trim().parse()` (문자열 → 숫자)\n- **타입 변환**: `as` 키워드, `.parse()` 메서드"
    },
    {
      "chapterId": 4,
      "title": "4단원: 조건문",
      "content": "# 4단원: 조건문\n\n## if 문\n\n```rust\nlet number = 5;\n\nif number > 0 {\n    println!(\"양수입니다\");\n}\n```\n\n**조건은 반드시 `bool` 타입이어야 합니다:**\n\n```rust\n// if number { }  // 에러! Rust는 자동 타입 변환 없음\nif number != 0 {\n    println!(\"0이 아닙니다\");\n}\n```\n\n## if-else 문\n\n```rust\nlet age = 18;\n\nif age >= 20 {\n    println!(\"성인입니다\");\n} else {\n    println!(\"미성년자입니다\");\n}\n```\n\n## else if 문\n\n```rust\nlet score = 85;\n\nif score >= 90 {\n    println!(\"A\");\n} else if score >= 80 {\n    println!(\"B\");\n} else if score >= 70 {\n    println!(\"C\");\n} else {\n    println!(\"F\");\n}\n```\n\n## if는 표현식\n\nRust의 `if`는 값을 반환할 수 있습니다:\n\n```rust\nlet condition = true;\nlet number = if condition { 5 } else { 10 };\nprintln!(\"number: {}\", number);  // 5\n```\n\n**타입이 일치해야 합니다:**\n\n```rust\n// let number = if condition { 5 } else { \"six\" };  // 에러!\nlet number = if condition { 5 } else { 6 };  // OK\n```\n\n## match 표현식\n\n`match`는 Rust의 강력한 패턴 매칭 도구입니다:\n\n```rust\nlet number = 3;\n\nmatch number {\n    1 => println!(\"일\"),\n    2 => println!(\"이\"),\n    3 => println!(\"삼\"),\n    _ => println!(\"기타\"),  // _ 는 모든 경우를 의미\n}\n```\n\n**값 반환:**\n\n```rust\nlet day = 3;\nlet day_name = match day {\n    1 => \"월요일\",\n    2 => \"화요일\",\n    3 => \"수요일\",\n    4 => \"목요일\",\n    5 => \"금요일\",\n    6 | 7 => \"주말\",  // 여러 패턴\n    _ => \"잘못된 입력\",\n};\n```\n\n**범위 매칭:**\n\n```rust\nlet age = 25;\n\nmatch age {\n    0..=12 => println!(\"어린이\"),\n    13..=19 => println!(\"청소년\"),\n    20..=64 => println!(\"성인\"),\n    _ => println!(\"노인\"),\n}\n```\n\n## if let 표현식\n\n하나의 패턴만 매칭할 때 간결하게 작성:\n\n```rust\nlet some_value = Some(7);\n\nif let Some(7) = some_value {\n    println!(\"7입니다\");\n}\n```\n\n## 핵심 요약\n\n- **if 문**: `if 조건 { }` (조건은 반드시 `bool`)\n- **if-else**: `if 조건 { } else { }`\n- **else if**: 여러 조건 체크\n- **if는 표현식**: 값을 반환 가능\n- **match**: 패턴 매칭, 모든 경우 처리 필수 (exhaustive)\n- **match 패턴**: 단일 값, 여러 값 `|`, 범위 `..=`, 와일드카드 `_`\n- **if let**: 단일 패턴 매칭을 간결하게"
    },
    {
      "chapterId": 5,
      "title": "5단원: 반복문",
      "content": "# 5단원: 반복문\n\n## loop 무한 반복\n\n`loop`는 명시적으로 중단하지 않으면 무한 반복합니다:\n\n```rust\nloop {\n    println!(\"무한 반복\");\n    break;  // 반복 중단\n}\n```\n\n**값 반환:**\n\n```rust\nlet mut counter = 0;\nlet result = loop {\n    counter += 1;\n    if counter == 10 {\n        break counter * 2;  // 20 반환\n    }\n};\n```\n\n## while 조건 반복\n\n```rust\nlet mut number = 3;\n\nwhile number != 0 {\n    println!(\"{}\", number);\n    number -= 1;\n}\nprintln!(\"발사!\");\n```\n\n## for 반복문\n\n**범위 반복:**\n\n```rust\nfor i in 1..5 {\n    println!(\"{}\", i);  // 1, 2, 3, 4 (5 미포함)\n}\n\nfor i in 1..=5 {\n    println!(\"{}\", i);  // 1, 2, 3, 4, 5 (5 포함)\n}\n```\n\n**역순:**\n\n```rust\nfor i in (1..5).rev() {\n    println!(\"{}\", i);  // 4, 3, 2, 1\n}\n```\n\n**배열 반복:**\n\n```rust\nlet arr = [10, 20, 30, 40, 50];\n\nfor element in arr {\n    println!(\"값: {}\", element);\n}\n```\n\n**인덱스와 함께:**\n\n```rust\nfor (index, value) in arr.iter().enumerate() {\n    println!(\"{}번째: {}\", index, value);\n}\n```\n\n## break와 continue\n\n**break**: 반복문 즉시 종료\n\n```rust\nfor i in 1..10 {\n    if i == 5 {\n        break;\n    }\n    println!(\"{}\", i);  // 1, 2, 3, 4\n}\n```\n\n**continue**: 현재 반복 건너뛰기\n\n```rust\nfor i in 1..6 {\n    if i % 2 == 0 {\n        continue;\n    }\n    println!(\"{}\", i);  // 1, 3, 5\n}\n```\n\n## 라벨이 있는 반복문\n\n중첩된 반복문에서 특정 반복문을 제어:\n\n```rust\n'outer: for i in 1..4 {\n    for j in 1..4 {\n        if i * j > 5 {\n            break 'outer;  // 외부 반복문 종료\n        }\n        println!(\"i: {}, j: {}\", i, j);\n    }\n}\n```\n\n## Iterator 메서드\n\nRust의 반복자는 다양한 메서드를 제공합니다:\n\n```rust\nlet sum: i32 = (1..=10).sum();  // 1+2+...+10 = 55\n\nlet doubled: Vec<i32> = (1..5).map(|x| x * 2).collect();\n// [2, 4, 6, 8]\n\nlet evens: Vec<i32> = (1..10).filter(|x| x % 2 == 0).collect();\n// [2, 4, 6, 8]\n```\n\n## 핵심 요약\n\n- **loop**: 무한 반복, `break`로 종료, 값 반환 가능\n- **while**: `while 조건 { }` 조건이 true인 동안 반복\n- **for**: `for 변수 in 범위 { }` 범위를 순회\n- **범위**: `1..5` (5 미포함), `1..=5` (5 포함)\n- **역순**: `.rev()` 메서드\n- **break**: 반복 즉시 종료\n- **continue**: 현재 반복 건너뛰기\n- **라벨**: `'이름:` 으로 중첩 반복문 제어\n- **Iterator**: `.sum()`, `.map()`, `.filter()` 등 함수형 메서드"
    },
    {
      "chapterId": 6,
      "title": "6단원: 함수와 소유권",
      "content": "# 6단원: 함수와 소유권\n\n## 함수 정의\n\n```rust\nfn greet() {\n    println!(\"안녕하세요!\");\n}\n\nfn main() {\n    greet();\n}\n```\n\n## 매개변수\n\n```rust\nfn print_number(x: i32) {\n    println!(\"숫자: {}\", x);\n}\n\nfn add(a: i32, b: i32) {\n    println!(\"합: {}\", a + b);\n}\n```\n\n## 반환값\n\n```rust\nfn add(a: i32, b: i32) -> i32 {\n    a + b  // 세미콜론 없음 = 반환\n}\n\nfn subtract(a: i32, b: i32) -> i32 {\n    return a - b;  // return 키워드 사용 가능\n}\n```\n\n## 표현식 vs 문장\n\n- **표현식(Expression)**: 값을 반환 (세미콜론 없음)\n- **문장(Statement)**: 값을 반환하지 않음 (세미콜론 있음)\n\n```rust\nfn example() -> i32 {\n    let x = 5;        // 문장\n    x + 1             // 표현식 (반환됨)\n}\n```\n\n## 소유권 (Ownership)\n\nRust의 핵심 개념으로, 메모리 안전성을 보장합니다.\n\n**소유권 규칙:**\n1. 각 값은 하나의 소유자(owner)를 가짐\n2. 한 번에 하나의 소유자만 존재\n3. 소유자가 스코프를 벗어나면 값이 drop됨\n\n```rust\nlet s1 = String::from(\"hello\");\nlet s2 = s1;  // s1의 소유권이 s2로 이동 (move)\n// println!(\"{}\", s1);  // 에러! s1은 더 이상 유효하지 않음\nprintln!(\"{}\", s2);  // OK\n```\n\n## 복사 vs 이동\n\n**Stack 데이터 (Copy):**\n\n```rust\nlet x = 5;\nlet y = x;  // 복사됨\nprintln!(\"x: {}, y: {}\", x, y);  // 둘 다 사용 가능\n```\n\n**Heap 데이터 (Move):**\n\n```rust\nlet s1 = String::from(\"hello\");\nlet s2 = s1;  // 이동됨\n// s1은 더 이상 사용 불가\n```\n\n## 참조와 빌림 (References & Borrowing)\n\n**불변 참조:**\n\n```rust\nfn calculate_length(s: &String) -> usize {\n    s.len()\n}\n\nlet s1 = String::from(\"hello\");\nlet len = calculate_length(&s1);\nprintln!(\"길이: {}\", len);\nprintln!(\"s1: {}\", s1);  // s1 여전히 사용 가능\n```\n\n**가변 참조:**\n\n```rust\nfn change(s: &mut String) {\n    s.push_str(\", world\");\n}\n\nlet mut s = String::from(\"hello\");\nchange(&mut s);\nprintln!(\"{}\", s);  // \"hello, world\"\n```\n\n**참조 규칙:**\n- 불변 참조는 여러 개 가능\n- 가변 참조는 하나만 가능\n- 불변 참조와 가변 참조는 동시에 존재 불가\n\n```rust\nlet mut s = String::from(\"hello\");\nlet r1 = &s;\nlet r2 = &s;  // OK\n// let r3 = &mut s;  // 에러! 불변 참조가 있는 동안 가변 참조 불가\n```\n\n## 슬라이스 (Slice)\n\n```rust\nlet s = String::from(\"hello world\");\nlet hello = &s[0..5];   // \"hello\"\nlet world = &s[6..11];  // \"world\"\nlet whole = &s[..];     // 전체\n```\n\n## 핵심 요약\n\n- **함수**: `fn 이름(매개변수: 타입) -> 반환타입 { }`\n- **반환**: 마지막 표현식(세미콜론 없음) 또는 `return`\n- **소유권**: 각 값은 하나의 소유자, 이동 시 이전 소유자는 무효화\n- **복사**: Stack 타입은 자동 복사 (`i32`, `bool` 등)\n- **이동**: Heap 타입은 소유권 이동 (`String`, `Vec` 등)\n- **참조**: `&` (불변), `&mut` (가변) - 소유권을 빌림\n- **참조 규칙**: 불변 여러 개 OR 가변 하나만\n- **슬라이스**: `&타입[시작..끝]` 데이터의 일부분 참조"
    },
    {
      "chapterId": 7,
      "title": "7단원: 컬렉션",
      "content": "# 7단원: 컬렉션\n\n## Vector\n\nVector는 동적 크기 배열입니다.\n\n**생성:**\n\n```rust\nlet v1: Vec<i32> = Vec::new();\nlet v2 = vec![1, 2, 3, 4, 5];  // 매크로 사용\n```\n\n**요소 추가/제거:**\n\n```rust\nlet mut v = vec![1, 2, 3];\nv.push(4);       // [1, 2, 3, 4]\nv.pop();         // [1, 2, 3] (4 제거하고 반환)\n```\n\n**접근:**\n\n```rust\nlet v = vec![1, 2, 3, 4, 5];\nlet third = &v[2];           // 3 (패닉 가능)\nlet third = v.get(2);        // Some(3) (안전)\n```\n\n**반복:**\n\n```rust\nlet v = vec![100, 200, 300];\nfor i in &v {\n    println!(\"{}\", i);\n}\n\n// 가변 반복\nlet mut v = vec![1, 2, 3];\nfor i in &mut v {\n    *i += 50;\n}\n```\n\n## String\n\nRust의 `String`은 UTF-8로 인코딩된 가변 문자열입니다.\n\n**생성:**\n\n```rust\nlet s1 = String::new();\nlet s2 = String::from(\"hello\");\nlet s3 = \"hello\".to_string();\n```\n\n**추가:**\n\n```rust\nlet mut s = String::from(\"foo\");\ns.push_str(\"bar\");   // \"foobar\"\ns.push('!');         // \"foobar!\"\n```\n\n**연결:**\n\n```rust\nlet s1 = String::from(\"Hello, \");\nlet s2 = String::from(\"world!\");\nlet s3 = s1 + &s2;   // s1은 이동됨\n\n// format! 매크로 사용 (소유권 이동 없음)\nlet s1 = String::from(\"tic\");\nlet s2 = String::from(\"tac\");\nlet s3 = String::from(\"toe\");\nlet s = format!(\"{}-{}-{}\", s1, s2, s3);\n```\n\n**인덱싱 불가:**\n\n```rust\nlet s = String::from(\"hello\");\n// let h = s[0];  // 에러! Rust는 문자열 인덱싱 불가\n\n// 슬라이스는 가능 (바이트 단위 주의)\nlet slice = &s[0..4];\n```\n\n**반복:**\n\n```rust\nfor c in \"नमस्ते\".chars() {\n    println!(\"{}\", c);  // 문자 단위\n}\n\nfor b in \"नमस्ते\".bytes() {\n    println!(\"{}\", b);  // 바이트 단위\n}\n```\n\n## HashMap\n\n키-값 쌍을 저장하는 해시맵입니다.\n\n**생성:**\n\n```rust\nuse std::collections::HashMap;\n\nlet mut scores = HashMap::new();\nscores.insert(String::from(\"Blue\"), 10);\nscores.insert(String::from(\"Yellow\"), 50);\n```\n\n**접근:**\n\n```rust\nlet team_name = String::from(\"Blue\");\nlet score = scores.get(&team_name);  // Some(&10)\n```\n\n**반복:**\n\n```rust\nfor (key, value) in &scores {\n    println!(\"{}: {}\", key, value);\n}\n```\n\n**덮어쓰기와 업데이트:**\n\n```rust\nscores.insert(String::from(\"Blue\"), 25);  // 덮어쓰기\n\n// 키가 없을 때만 삽입\nscores.entry(String::from(\"Red\")).or_insert(30);\n\n// 기존 값 기반 업데이트\nlet count = scores.entry(String::from(\"Blue\")).or_insert(0);\n*count += 1;\n```\n\n## 컬렉션 메서드\n\n**공통:**\n\n```rust\nlet v = vec![1, 2, 3, 4, 5];\nv.len();        // 길이\nv.is_empty();   // 비어있는지\nv.clear();      // 모두 제거\n```\n\n**Vector 메서드:**\n\n```rust\nv.contains(&3);     // 포함 여부\nv.sort();           // 정렬\nv.reverse();        // 뒤집기\n```\n\n## 핵심 요약\n\n- **Vector**: `Vec<T>` - 동적 배열, `vec![...]` 매크로\n- **Vector 메서드**: `.push()`, `.pop()`, `.get()`, `[]` 인덱싱\n- **String**: UTF-8 가변 문자열\n- **String 생성**: `String::from()`, `.to_string()`\n- **String 메서드**: `.push_str()`, `.push()`, `+` 연산, `format!`\n- **String 반복**: `.chars()` (문자), `.bytes()` (바이트)\n- **HashMap**: `HashMap<K, V>` - 키-값 저장소\n- **HashMap 메서드**: `.insert()`, `.get()`, `.entry().or_insert()`\n- **소유권**: 컬렉션에 값 추가 시 소유권 이동"
    },
    {
      "chapterId": 8,
      "title": "8단원: 구조체와 열거형",
      "content": "# 8단원: 구조체와 열거형\n\n## 구조체 (Struct)\n\n**정의:**\n\n```rust\nstruct User {\n    username: String,\n    email: String,\n    active: bool,\n    sign_in_count: u64,\n}\n```\n\n**인스턴스 생성:**\n\n```rust\nlet user1 = User {\n    email: String::from(\"user@example.com\"),\n    username: String::from(\"user123\"),\n    active: true,\n    sign_in_count: 1,\n};\n```\n\n**필드 접근:**\n\n```rust\nlet email = user1.email;\n\nlet mut user2 = User {\n    email: String::from(\"another@example.com\"),\n    username: String::from(\"another\"),\n    active: true,\n    sign_in_count: 1,\n};\nuser2.email = String::from(\"new@example.com\");\n```\n\n**구조체 업데이트 문법:**\n\n```rust\nlet user3 = User {\n    email: String::from(\"user3@example.com\"),\n    ..user1  // 나머지 필드는 user1에서 복사\n};\n```\n\n**튜플 구조체:**\n\n```rust\nstruct Color(i32, i32, i32);\nstruct Point(i32, i32, i32);\n\nlet black = Color(0, 0, 0);\nlet origin = Point(0, 0, 0);\n```\n\n## 메서드\n\n```rust\nstruct Rectangle {\n    width: u32,\n    height: u32,\n}\n\nimpl Rectangle {\n    // 메서드\n    fn area(&self) -> u32 {\n        self.width * self.height\n    }\n    \n    fn can_hold(&self, other: &Rectangle) -> bool {\n        self.width > other.width && self.height > other.height\n    }\n    \n    // 연관 함수 (생성자로 자주 사용)\n    fn square(size: u32) -> Rectangle {\n        Rectangle {\n            width: size,\n            height: size,\n        }\n    }\n}\n\nlet rect = Rectangle { width: 30, height: 50 };\nprintln!(\"면적: {}\", rect.area());\n\nlet sq = Rectangle::square(20);\n```\n\n## 열거형 (Enum)\n\n**정의:**\n\n```rust\nenum IpAddrKind {\n    V4,\n    V6,\n}\n\nlet four = IpAddrKind::V4;\nlet six = IpAddrKind::V6;\n```\n\n**데이터를 포함하는 열거형:**\n\n```rust\nenum IpAddr {\n    V4(u8, u8, u8, u8),\n    V6(String),\n}\n\nlet home = IpAddr::V4(127, 0, 0, 1);\nlet loopback = IpAddr::V6(String::from(\"::1\"));\n```\n\n**다양한 타입:**\n\n```rust\nenum Message {\n    Quit,\n    Move { x: i32, y: i32 },\n    Write(String),\n    ChangeColor(i32, i32, i32),\n}\n```\n\n## Option 열거형\n\nRust에는 `null`이 없습니다. 대신 `Option`을 사용합니다:\n\n```rust\nenum Option<T> {\n    Some(T),\n    None,\n}\n\nlet some_number = Some(5);\nlet some_string = Some(\"a string\");\nlet absent_number: Option<i32> = None;\n```\n\n**사용:**\n\n```rust\nlet x: i32 = 5;\nlet y: Option<i32> = Some(5);\n\n// let sum = x + y;  // 에러! Option<i32>와 i32는 다른 타입\n\nif let Some(value) = y {\n    let sum = x + value;  // OK\n}\n```\n\n## Result 열거형\n\n에러 처리에 사용됩니다:\n\n```rust\nenum Result<T, E> {\n    Ok(T),\n    Err(E),\n}\n```\n\n**사용:**\n\n```rust\nuse std::fs::File;\n\nlet f = File::open(\"hello.txt\");\n\nlet f = match f {\n    Ok(file) => file,\n    Err(error) => panic!(\"파일 열기 실패: {:?}\", error),\n};\n```\n\n## 패턴 매칭\n\n```rust\nenum Coin {\n    Penny,\n    Nickel,\n    Dime,\n    Quarter,\n}\n\nfn value_in_cents(coin: Coin) -> u8 {\n    match coin {\n        Coin::Penny => 1,\n        Coin::Nickel => 5,\n        Coin::Dime => 10,\n        Coin::Quarter => 25,\n    }\n}\n```\n\n**값 추출:**\n\n```rust\nfn plus_one(x: Option<i32>) -> Option<i32> {\n    match x {\n        None => None,\n        Some(i) => Some(i + 1),\n    }\n}\n```\n\n## 핵심 요약\n\n- **구조체**: `struct 이름 { 필드: 타입 }` - 관련 데이터 그룹화\n- **메서드**: `impl 구조체 { fn 메서드(&self) { } }`\n- **연관 함수**: `self` 없는 함수, `::` 로 호출\n- **튜플 구조체**: `struct 이름(타입들);`\n- **열거형**: `enum 이름 { 변형들 }` - 여러 가능한 값 중 하나\n- **데이터 포함**: 각 변형이 다른 타입의 데이터 보유 가능\n- **Option**: `Some(T)` 또는 `None` - null 대체\n- **Result**: `Ok(T)` 또는 `Err(E)` - 에러 처리\n- **match**: 열거형 패턴 매칭, 모든 경우 처리 필수"
    },
    {
      "chapterId": 9,
      "title": "9단원: 모듈과 패키지",
      "content": "# 9단원: 모듈과 패키지\n\n## 모듈 (Module)\n\n모듈은 코드를 구조화하고 캡슐화합니다.\n\n**정의:**\n\n```rust\nmod front_of_house {\n    pub mod hosting {\n        pub fn add_to_waitlist() {}\n    }\n}\n\npub fn eat_at_restaurant() {\n    // 절대 경로\n    crate::front_of_house::hosting::add_to_waitlist();\n    \n    // 상대 경로\n    front_of_house::hosting::add_to_waitlist();\n}\n```\n\n## 공개/비공개\n\n- 기본적으로 모든 항목은 비공개(private)\n- `pub` 키워드로 공개(public)로 만들 수 있음\n\n```rust\nmod outer {\n    pub fn public_function() {}\n    fn private_function() {}\n    \n    pub mod inner {\n        pub fn inner_public() {}\n        fn inner_private() {}\n    }\n}\n```\n\n## use 키워드\n\n경로를 짧게 만듭니다:\n\n```rust\nuse crate::front_of_house::hosting;\n\npub fn eat_at_restaurant() {\n    hosting::add_to_waitlist();\n}\n```\n\n**as로 별칭:**\n\n```rust\nuse std::fmt::Result;\nuse std::io::Result as IoResult;\n```\n\n**재내보내기:**\n\n```rust\npub use crate::front_of_house::hosting;\n```\n\n**중첩된 경로:**\n\n```rust\nuse std::io::{self, Write};\nuse std::collections::{HashMap, BTreeMap, HashSet};\n```\n\n**glob:**\n\n```rust\nuse std::collections::*;\n```\n\n## 파일로 분리\n\n**src/lib.rs:**\n\n```rust\nmod front_of_house;\n\npub use crate::front_of_house::hosting;\n```\n\n**src/front_of_house.rs:**\n\n```rust\npub mod hosting {\n    pub fn add_to_waitlist() {}\n}\n```\n\n**또는 src/front_of_house/mod.rs:**\n\n```rust\npub mod hosting;\n```\n\n**src/front_of_house/hosting.rs:**\n\n```rust\npub fn add_to_waitlist() {}\n```\n\n## 패키지와 크레이트\n\n**패키지 (Package):**\n- Cargo.toml 파일 포함\n- 하나 이상의 크레이트를 포함\n\n**크레이트 (Crate):**\n- 바이너리 크레이트: 실행 파일 (src/main.rs)\n- 라이브러리 크레이트: 라이브러리 (src/lib.rs)\n\n**새 패키지 생성:**\n\n```bash\ncargo new my_project\ncargo new --lib my_library\n```\n\n## 외부 크레이트 사용\n\n**Cargo.toml:**\n\n```toml\n[dependencies]\nrand = \"0.8.5\"\nserde = { version = \"1.0\", features = [\"derive\"] }\n```\n\n**코드에서 사용:**\n\n```rust\nuse rand::Rng;\n\nfn main() {\n    let num = rand::thread_rng().gen_range(1..=100);\n}\n```\n\n## 워크스페이스 (Workspace)\n\n여러 패키지를 하나의 워크스페이스로 관리:\n\n**Cargo.toml (루트):**\n\n```toml\n[workspace]\nmembers = [\n    \"adder\",\n    \"add_one\",\n]\n```\n\n## super와 self\n\n**super**: 부모 모듈 참조\n\n```rust\nfn serve_order() {}\n\nmod back_of_house {\n    fn cook_order() {}\n    \n    fn fix_incorrect_order() {\n        cook_order();\n        super::serve_order();  // 부모의 함수 호출\n    }\n}\n```\n\n**self**: 현재 모듈\n\n```rust\nuse self::front_of_house::hosting;\n```\n\n## 핵심 요약\n\n- **모듈**: `mod 이름 { }` - 코드 조직화\n- **공개**: `pub` 키워드로 외부 접근 허용\n- **경로**: 절대(`crate::`), 상대, `super::`, `self::`\n- **use**: 경로 가져오기, `as`로 별칭\n- **pub use**: 재내보내기\n- **파일 분리**: `mod 이름;`으로 다른 파일 불러오기\n- **패키지**: Cargo.toml + 크레이트들\n- **크레이트**: 바이너리(실행 파일) 또는 라이브러리\n- **외부 크레이트**: Cargo.toml에 의존성 추가\n- **워크스페이스**: 여러 패키지 통합 관리"
    },
    {
      "chapterId": 10,
      "title": "10단원: 에러 처리와 제네릭",
      "content": "# 10단원: 에러 처리와 제네릭\n\n## 에러 처리\n\nRust는 두 가지 에러 카테고리가 있습니다:\n- **복구 가능한 에러**: `Result<T, E>`\n- **복구 불가능한 에러**: `panic!`\n\n### panic!\n\n프로그램을 즉시 종료합니다:\n\n```rust\npanic!(\"크래시 발생!\");\n```\n\n### Result\n\n```rust\nuse std::fs::File;\nuse std::io::ErrorKind;\n\nlet f = File::open(\"hello.txt\");\n\nlet f = match f {\n    Ok(file) => file,\n    Err(error) => match error.kind() {\n        ErrorKind::NotFound => match File::create(\"hello.txt\") {\n            Ok(fc) => fc,\n            Err(e) => panic!(\"파일 생성 실패: {:?}\", e),\n        },\n        other_error => panic!(\"파일 열기 실패: {:?}\", other_error),\n    },\n};\n```\n\n### unwrap과 expect\n\n```rust\nlet f = File::open(\"hello.txt\").unwrap();\nlet f = File::open(\"hello.txt\").expect(\"파일 열기 실패\");\n```\n\n### 에러 전파\n\n**? 연산자:**\n\n```rust\nuse std::io;\nuse std::io::Read;\nuse std::fs::File;\n\nfn read_username_from_file() -> Result<String, io::Error> {\n    let mut f = File::open(\"username.txt\")?;\n    let mut s = String::new();\n    f.read_to_string(&mut s)?;\n    Ok(s)\n}\n```\n\n**더 짧게:**\n\n```rust\nfn read_username_from_file() -> Result<String, io::Error> {\n    let mut s = String::new();\n    File::open(\"username.txt\")?.read_to_string(&mut s)?;\n    Ok(s)\n}\n```\n\n## 제네릭 (Generics)\n\n### 함수\n\n```rust\nfn largest<T: PartialOrd>(list: &[T]) -> &T {\n    let mut largest = &list[0];\n    for item in list {\n        if item > largest {\n            largest = item;\n        }\n    }\n    largest\n}\n```\n\n### 구조체\n\n```rust\nstruct Point<T> {\n    x: T,\n    y: T,\n}\n\nlet integer = Point { x: 5, y: 10 };\nlet float = Point { x: 1.0, y: 4.0 };\n```\n\n**여러 타입 매개변수:**\n\n```rust\nstruct Point<T, U> {\n    x: T,\n    y: U,\n}\n\nlet p = Point { x: 5, y: 4.0 };\n```\n\n### 열거형\n\n```rust\nenum Option<T> {\n    Some(T),\n    None,\n}\n\nenum Result<T, E> {\n    Ok(T),\n    Err(E),\n}\n```\n\n### 메서드\n\n```rust\nimpl<T> Point<T> {\n    fn x(&self) -> &T {\n        &self.x\n    }\n}\n\n// 특정 타입에만 구현\nimpl Point<f32> {\n    fn distance_from_origin(&self) -> f32 {\n        (self.x.powi(2) + self.y.powi(2)).sqrt()\n    }\n}\n```\n\n## 트레이트 (Traits)\n\n트레이트는 공유 가능한 동작을 정의합니다.\n\n**정의:**\n\n```rust\npub trait Summary {\n    fn summarize(&self) -> String;\n}\n```\n\n**구현:**\n\n```rust\npub struct NewsArticle {\n    pub headline: String,\n    pub content: String,\n}\n\nimpl Summary for NewsArticle {\n    fn summarize(&self) -> String {\n        format!(\"{}: {}\", self.headline, self.content)\n    }\n}\n```\n\n**기본 구현:**\n\n```rust\npub trait Summary {\n    fn summarize(&self) -> String {\n        String::from(\"(더 읽기...)\")\n    }\n}\n```\n\n**트레이트 바운드:**\n\n```rust\npub fn notify<T: Summary>(item: &T) {\n    println!(\"속보! {}\", item.summarize());\n}\n\n// impl Trait 문법\npub fn notify(item: &impl Summary) {\n    println!(\"속보! {}\", item.summarize());\n}\n\n// 여러 트레이트\npub fn notify<T: Summary + Display>(item: &T) {}\n\n// where 절\nfn some_function<T, U>(t: &T, u: &U) -> i32\n    where T: Display + Clone,\n          U: Clone + Debug\n{\n    // ...\n}\n```\n\n**트레이트 반환:**\n\n```rust\nfn returns_summarizable() -> impl Summary {\n    NewsArticle {\n        headline: String::from(\"제목\"),\n        content: String::from(\"내용\"),\n    }\n}\n```\n\n## 라이프타임 (Lifetimes)\n\n참조가 유효한 범위를 명시합니다:\n\n```rust\nfn longest<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() {\n        x\n    } else {\n        y\n    }\n}\n```\n\n**구조체:**\n\n```rust\nstruct ImportantExcerpt<'a> {\n    part: &'a str,\n}\n```\n\n**정적 라이프타임:**\n\n```rust\nlet s: &'static str = \"전체 프로그램 기간 동안 유효\";\n```\n\n## 핵심 요약\n\n- **panic!**: 복구 불가능 에러, 프로그램 종료\n- **Result<T, E>**: `Ok(T)` 또는 `Err(E)`, 복구 가능 에러\n- **unwrap/expect**: Result를 간단히 처리 (에러 시 panic)\n- **? 연산자**: 에러 자동 전파\n- **제네릭**: `<T>` 타입 매개변수로 재사용 가능한 코드\n- **트레이트**: 공유 동작 정의, 다형성 제공\n- **트레이트 바운드**: `<T: Trait>` 제네릭 타입 제약\n- **라이프타임**: `<'a>` 참조 유효 범위 명시\n- **성능**: 제네릭은 컴파일 타임에 단형화(monomorphization)되어 런타임 비용 없음"
    }
  ]
}
