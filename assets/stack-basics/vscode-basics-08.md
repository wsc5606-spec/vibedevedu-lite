# Chapter 8: 터미널 사용

VS Code에 통합된 터미널을 사용하면 창을 왔다갔다 할 필요가 없어요!

## 8.1 통합 터미널이란?

VS Code 내부에서 실행되는 명령줄 인터페이스입니다.

**장점:**
- 에디터와 터미널을 동시에 사용
- 창 전환 없이 작업
- 프로젝트 폴더에서 자동 시작
- 여러 터미널 동시 실행
- 출력 결과를 바로 확인

```text
바데부: "터미널이 VS Code 안에 있으니까 완전 편해!"
```

## 8.2 터미널 열기

**열기 방법:**

**방법 1: 단축키**
- `` Ctrl + ` `` (백틱)
- `Ctrl + Shift + ` ` (새 터미널)

**방법 2: 메뉴**
- Terminal → New Terminal

**방법 3: 커맨드 팔레트**
- `Ctrl + Shift + P` → "Terminal: Create New Terminal"

**방법 4: 우클릭**
- Explorer에서 폴더 우클릭 → "Open in Integrated Terminal"

```text
TERMINAL
┌──────────────────────────────┐
│ $ _                          │  ← 커서
│                              │
└──────────────────────────────┘
```

```text
바데부: "Ctrl + ` 단축키만 알아도 언제든지 터미널을 열 수 있어!"
```

## 8.3 기본 터미널 명령어

**파일/폴더 관리:**
```bash
# 현재 위치 확인
pwd

# 파일 목록 보기
ls          # Mac/Linux
dir         # Windows

# 폴더 이동
cd Desktop
cd ..       # 상위 폴더
cd ~        # 홈 폴더

# 폴더 생성
mkdir my-folder

# 파일 생성
touch file.txt    # Mac/Linux
echo. > file.txt  # Windows

# 파일 삭제
rm file.txt       # Mac/Linux
del file.txt      # Windows

# 폴더 삭제
rm -rf folder     # Mac/Linux
rmdir /s folder   # Windows
```

**바데부의 자주 쓰는 명령어:**
```bash
# Git
git status
git add .
git commit -m "메시지"
git push

# npm
npm install
npm run dev
npm run build

# Python
python app.py
pip install requests
python -m venv venv

# 기타
code .           # VS Code로 현재 폴더 열기
clear            # 화면 지우기
```

```text
바데부: "이 명령어들만 알아도 대부분의 작업을 할 수 있어!"
```

## 8.4 여러 터미널 관리

동시에 여러 터미널을 실행할 수 있습니다.

**새 터미널 추가:**
- `Ctrl + Shift + ` `
- 터미널 패널의 + 아이콘

**터미널 전환:**
- 드롭다운에서 선택
- `Ctrl + PageUp/PageDown`

**터미널 분할:**
- 터미널 패널의 분할 아이콘
- `Ctrl + Shift + 5`

```text
┌──────────────┬──────────────┐
│ Terminal 1   │ Terminal 2   │
│ $ npm run dev│ $ git status │
└──────────────┴──────────────┘
```

**터미널 이름 변경:**
1. 터미널 우클릭
2. "Rename" 선택
3. 새 이름 입력

```text
예시:
├─ 📟 개발 서버
├─ 📟 테스트
└─ 📟 Git
```

```text
바데부: "개발 서버, Git, 테스트용으로 터미널을 3개 띄워놓고 써!"
```

## 8.5 터미널 종류 선택

다양한 셸을 사용할 수 있습니다.

**Windows:**
- PowerShell (기본)
- Command Prompt (cmd)
- Git Bash
- WSL (Ubuntu 등)

**macOS/Linux:**
- bash (기본)
- zsh
- fish
- sh

**터미널 변경:**
1. 터미널 드롭다운 클릭
2. "Select Default Profile" 선택
3. 원하는 셸 선택

**또는 설정:**
```json
{
  "terminal.integrated.defaultProfile.windows": "Git Bash",
  "terminal.integrated.defaultProfile.osx": "zsh"
}
```

```text
바데부: "나는 Git Bash를 쓰는데, 명령어가 Mac/Linux랑 똑같아서 편해!"
```

## 8.6 터미널 커스터마이징

**글꼴 변경:**
```json
{
  "terminal.integrated.fontFamily": "Consolas, 'Courier New'",
  "terminal.integrated.fontSize": 14,
  "terminal.integrated.lineHeight": 1.2
}
```

**커서 스타일:**
```json
{
  "terminal.integrated.cursorStyle": "block",
  "terminal.integrated.cursorBlinking": true
}
```

**테마:**
```json
{
  "workbench.colorCustomizations": {
    "terminal.background": "#1E1E1E",
    "terminal.foreground": "#CCCCCC",
    "terminal.ansiBlack": "#000000",
    "terminal.ansiRed": "#E06C75",
    "terminal.ansiGreen": "#98C379",
    "terminal.ansiYellow": "#E5C07B",
    "terminal.ansiBlue": "#61AFEF"
  }
}
```

**스크롤백 버퍼:**
```json
{
  "terminal.integrated.scrollback": 10000
}
```

```text
바데부: "터미널도 내 취향대로 꾸밀 수 있어!"
```

## 8.7 터미널에서 에디터 제어

터미널에서 파일을 VS Code로 열 수 있습니다.

**파일 열기:**
```bash
# 파일 열기
code file.txt

# 현재 폴더 열기
code .

# 새 창으로 열기
code -n folder

# 파일 비교
code --diff file1.txt file2.txt

# 특정 줄로 이동
code -g file.txt:10
```

**예시 워크플로우:**
```bash
# 1. 프로젝트 생성
mkdir my-app
cd my-app

# 2. 파일 생성
touch index.html

# 3. VS Code로 열기
code .

# 4. 또는 바로 파일 열기
code index.html
```

```text
바데부: "'code .' 명령어는 정말 자주 써! 프로젝트를 빠르게 열 수 있거든."
```

## 8.8 터미널 단축키

효율적인 터미널 사용을 위한 단축키입니다.

**기본 단축키:**
```text
Ctrl + `          - 터미널 토글
Ctrl + Shift + `  - 새 터미널
Ctrl + Shift + 5  - 터미널 분할
Ctrl + Shift + C  - 복사
Ctrl + Shift + V  - 붙여넣기
Ctrl + L          - 화면 지우기
Ctrl + C          - 명령 중단
```

**터미널 내 이동:**
```text
Ctrl + A          - 줄 시작으로
Ctrl + E          - 줄 끝으로
Ctrl + U          - 줄 삭제
Ctrl + K          - 커서부터 끝까지 삭제
Ctrl + W          - 이전 단어 삭제
```

**히스토리:**
```text
↑/↓              - 이전/다음 명령어
Ctrl + R          - 명령어 검색
!!                - 이전 명령어 재실행
```

```text
바데부: "이 단축키들을 쓰면 터미널 작업 속도가 훨씬 빨라져!"
```

## 8.9 Task Runner

반복 작업을 자동화할 수 있습니다.

**tasks.json 만들기:**
1. `Ctrl + Shift + P` → "Tasks: Configure Task"
2. 템플릿 선택
3. tasks.json 편집

**예시: npm 스크립트**
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "개발 서버 실행",
      "type": "npm",
      "script": "dev",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
    {
      "label": "빌드",
      "type": "npm",
      "script": "build"
    },
    {
      "label": "테스트",
      "type": "npm",
      "script": "test"
    }
  ]
}
```

**Task 실행:**
- `Ctrl + Shift + B` (기본 빌드 태스크)
- `Ctrl + Shift + P` → "Tasks: Run Task" → 태스크 선택

**예시: Python 스크립트**
```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Python 실행",
      "type": "shell",
      "command": "python",
      "args": ["${file}"],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
```

```text
바데부: "Task Runner로 명령어를 저장해두면 클릭 한 번에 실행할 수 있어!"
```

## 8.10 출력 패널

확장 프로그램과 작업의 출력을 보여줍니다.

**출력 패널 열기:**
- `Ctrl + Shift + U`
- 패널의 "Output" 탭

**출력 소스 선택:**
```text
OUTPUT
┌──────────────────────────────┐
│ ▼ Git                        │  ← 드롭다운
│                              │
│ Cloning repository...        │
│ POST git-upload-pack (175 ms)│
│ Unpacking objects: 100%      │
└──────────────────────────────┘
```

**주요 출력 소스:**
- Git
- Tasks
- Extensions
- Language Servers
- Debug

```text
바데부: "뭔가 안 될 때 Output 패널을 보면 원인을 알 수 있어!"
```

## 8.11 Problems 패널

코드 오류와 경고를 한눈에 봅니다.

**열기:**
- `Ctrl + Shift + M`
- 패널의 "Problems" 탭

```text
PROBLEMS
┌──────────────────────────────┐
│ ⚠ 2 warnings  ❌ 1 error     │
│                              │
│ ❌ src/app.js (line 10)      │
│   'userName' is not defined  │
│                              │
│ ⚠ src/style.css (line 5)    │
│   Unknown property 'colro'   │
└──────────────────────────────┘
```

**기능:**
- 클릭하면 해당 위치로 이동
- 필터링 (오류만/경고만)
- 파일별 그룹화

**문제 해결 팁:**
```javascript
// ❌ 오류: userName이 정의되지 않음
console.log(userName);

// ✅ 수정:
const userName = "바데부";
console.log(userName);
```

```text
바데부: "Problems 패널을 보면 어디를 고쳐야 할지 바로 알 수 있어!"
```

## 8.12 실습: 터미널 워크플로우

바데부와 함께 실전 터미널 사용법을 익혀봅시다!

**프로젝트 1: Node.js 프로젝트**
```bash
# 1. 프로젝트 생성
mkdir my-node-app
cd my-node-app

# 2. npm 초기화
npm init -y

# 3. 패키지 설치
npm install express

# 4. VS Code 열기
code .

# 5. 서버 파일 생성 (VS Code에서)
# app.js 생성

# 6. 개발 서버 실행
node app.js
```

**프로젝트 2: Python 프로젝트**
```bash
# 1. 가상환경 생성
python -m venv venv

# 2. 가상환경 활성화
# Windows
venv\Scripts\activate
# Mac/Linux
source venv/bin/activate

# 3. 패키지 설치
pip install flask

# 4. 앱 실행
python app.py
```

**프로젝트 3: Git + 개발**
```text
터미널 1 (개발 서버):
$ npm run dev

터미널 2 (Git):
$ git status
$ git add .
$ git commit -m "기능 추가"

터미널 3 (테스트):
$ npm test
```

```text
바데부: "여러 터미널을 동시에 쓰면 작업이 훨씬 효율적이야!"
```

## 8.13 터미널 팁과 트릭

**팁 1: 명령어 히스토리 검색**
```bash
# Ctrl + R을 누르고 검색어 입력
(reverse-i-search)`npm': npm run dev
```

**팁 2: 여러 명령어 실행**
```bash
# && : 이전 명령 성공 시 다음 실행
npm install && npm run build

# ; : 이전 명령 성공 여부 상관없이 다음 실행
git add . ; git status

# || : 이전 명령 실패 시 다음 실행
npm run build || echo "Build failed"
```

**팁 3: 환경 변수 설정**
```bash
# Windows PowerShell
$env:PORT=3000

# Windows CMD
set PORT=3000

# Mac/Linux
export PORT=3000

# 사용
node app.js
```

**팁 4: 별칭(Alias) 설정**
```bash
# Mac/Linux (.bashrc 또는 .zshrc)
alias gs="git status"
alias gp="git push"
alias dev="npm run dev"

# 사용
gs  # git status와 동일
```

**팁 5: 터미널 탭 고정**
```json
{
  "terminal.integrated.tabs.enabled": true,
  "terminal.integrated.tabs.hideCondition": "never"
}
```

```text
바데부: "이런 팁들을 쓰면 터미널을 더 편하게 쓸 수 있어!"
```

## 8.14 WSL (Windows Subsystem for Linux)

Windows에서 Linux를 사용할 수 있습니다.

**WSL 설치 (Windows 10/11):**
```powershell
# PowerShell 관리자 권한으로 실행
wsl --install
```

**VS Code에서 WSL 사용:**
1. "Remote - WSL" 확장 설치
2. `Ctrl + Shift + P` → "WSL: New Window"
3. Linux 환경에서 VS Code 실행!

**장점:**
- Windows에서 Linux 명령어 사용
- Linux 개발 환경
- Docker 등 Linux 도구 사용

```text
바데부: "WSL을 쓰면 Windows에서도 Linux 개발이 가능해!"
```

## 8.15 원격 개발

SSH로 원격 서버에서 개발할 수 있습니다.

**Remote - SSH 확장:**
1. Extensions에서 "Remote - SSH" 검색
2. 설치
3. `Ctrl + Shift + P` → "Remote-SSH: Connect to Host"
4. SSH 주소 입력 (예: user@server.com)

**장점:**
- 로컬처럼 원격 서버 사용
- 서버의 파일 직접 편집
- 원격 터미널 실행

```bash
# SSH 연결 예시
ssh badebu@192.168.1.100
```

```text
바데부: "원격 서버에서 작업해야 할 때 이 기능이 진짜 유용해!"
```

## 요약

이번 챕터에서 배운 내용:
- ✅ 통합 터미널 사용법
- ✅ 여러 터미널 관리
- ✅ 기본 명령어와 단축키
- ✅ Task Runner 활용
- ✅ Problems와 Output 패널

```text
바데부: "터미널을 VS Code 안에서 쓰니까 개발이 훨씬 편해졌어!
       다음 챕터에서는 단축키와 생산성 팁을 배울 거야!"
```

## 다음 챕터 예고

다음 챕터에서는:
- 필수 단축키 정리
- 커스텀 단축키 설정
- Emmet 고급 활용
- 스니펫 만들기
- 생산성 극대화 팁

**바데부와 함께 VS Code 고수 되기! 다음 편도 기대해줘!**
